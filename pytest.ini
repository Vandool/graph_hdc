[pytest]
; Enable live CLI logs at INFO level for matching tests
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

; Silence external libraries by default
filterwarnings =
    ignore::DeprecationWarning